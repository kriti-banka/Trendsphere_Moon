import 'package:flutter/material.dart';

class trends extends StatelessWidget {
  const trends({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(

          child: Image.asset('assets/images/trends.jpg'),

        )
    );
  }
}
